spring:
logging:
  level:
    org.springframework.web: DEBUG
server:
  hostname: localhost
  port: ${PORT:8080}
mail:
  server:
    host: smtp.gmail.com
    port: 587
    username: testsendmail1970@gmail.com
    password: ljznqcqlrxlweoek
    properties:mail:smtp: auth=true
    properties:mail:smtp:starttls: enable=true